# ─── API ──────────────────────────────────────────────────────────────────────
# Local: deixe vazio — o proxy do Vite encaminha /api → localhost:3001
VITE_API_BASE_URL=

# Produção (Render): URL completa da API, SEM barra final, COM /api
# VITE_API_BASE_URL=https://redeflow-api.onrender.com/api
